# Copy to .env.local and adjust if needed.
# Required for blog and Sanity Studio to work.

# Sanity (values match sanity.config.ts; create your own project at sanity.io for edits)
NEXT_PUBLIC_SANITY_PROJECT_ID=71ny25bn
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Optional: base URL for OG images and metadata (defaults to localhost when unset)
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: only needed for the contact form to send email (Resend)
# RESEND_API_KEY=re_xxxxx
